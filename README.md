A PHP script to generate a calendar (calendarbuild.php), style it (style.css), and populate it with user-defined events (events.php).
Each file is commented with explanations and tips for editing certain parts.
